import * as types from '@/store/types'

const state = {
  signInModalOpen: false
}
const getters = {}
const actions = {}
const mutations = {
  [types.SET_SIGNIN_MODAL_OPEN](state, payload) {
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}
